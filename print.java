class pu {
    public static void print(String text) {    // similar to def: in java - this is a function, instead of rewritting
        System.out.println(text); // code again and again, we can just call a function and it does it
        // for us, for eg this function makes it so that i dont have to type
        // as much for print statements
    }
}